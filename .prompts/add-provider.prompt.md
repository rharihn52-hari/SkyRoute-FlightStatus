# Add Provider

Using the existing codebase and IFlightStatusProvider interface:

Create a new provider implementation.

Requirements:

* Implement IFlightStatusProvider
* Add status normalization mappings
* Register provider in dependency injection
* Add unit tests
* Follow existing architecture and conventions
* Use async methods
* Include error handling and logging
